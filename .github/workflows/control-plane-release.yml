name: control-plane-release

on:
  push:
    tags: ["control-plane-v*"]

permissions:
  contents: write
  id-token: write
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          registry-url: "https://registry.npmjs.org"
          cache: npm
      - name: Verify immutable release version
        id: version
        shell: bash
        run: |
          set -euo pipefail
          VERSION="$(node -p 'require("./packages/control-plane/package.json").version')"
          test "$GITHUB_REF_NAME" = "control-plane-v$VERSION"
          if npm view "@octopus-reef/control-plane@$VERSION" version >/dev/null 2>&1; then
            echo "npm version already exists; refusing to replace an immutable release" >&2
            exit 1
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
      - run: npm ci
      - run: npm run verify:all
      - run: npm pack --dry-run --workspace @octopus-reef/control-plane
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Refuse to replace an existing image tag
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          if docker manifest inspect "ghcr.io/${GITHUB_REPOSITORY_OWNER}/octopus-reef-control-plane:${VERSION}" >/dev/null 2>&1; then
            echo "image tag already exists; refusing to replace an immutable release" >&2
            exit 1
          fi
      - name: Publish npm package
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          set -euo pipefail
          publish_if_missing() {
            local package="$1"
            local workspace="$2"
            local version
            version="$(node -p "require('./packages/${workspace}/package.json').version")"
            if npm view "${package}@${version}" version >/dev/null 2>&1; then
              echo "${package}@${version} is already immutable on npm; using it"
              return
            fi
            npm publish --access public --workspace "${package}"
          }

          # These are the minimum registry versions containing the cursor,
          # tool-idempotency and checkpoint/resume seams used by Control Plane.
          publish_if_missing "@octopus-reef/engine" "engine"
          publish_if_missing "@octopus-reef/protocol" "protocol"
          publish_if_missing "@octopus-reef/agent" "agent"
          npm publish --access public --workspace @octopus-reef/control-plane
      - name: Build and publish service image
        id: image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: packages/control-plane/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/octopus-reef-control-plane:${{ steps.version.outputs.version }}
            ghcr.io/${{ github.repository_owner }}/octopus-reef-control-plane:sha-${{ github.sha }}
          labels: |
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.version=${{ steps.version.outputs.version }}
            org.opencontainers.image.licenses=Apache-2.0
      - name: Create immutable GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ steps.version.outputs.version }}
          IMAGE_DIGEST: ${{ steps.image.outputs.digest }}
        run: |
          cat > "$RUNNER_TEMP/release-notes.md" <<EOF
          Published @octopus-reef/control-plane ${VERSION} to npm.

          Registry-compatible recovery prerequisites:
          - @octopus-reef/engine 0.2.1
          - @octopus-reef/protocol 0.1.1
          - @octopus-reef/agent 0.2.2

          HTTP v1 includes opaque baseline/project/work-item/acceptance refs,
          idempotent commands, decimal-cursor SSE, typed execution states and
          candidate diff/test/Evidence refs.

          Service image (pin this immutable digest):

          \`ghcr.io/${GITHUB_REPOSITORY_OWNER}/octopus-reef-control-plane@${IMAGE_DIGEST}\`

          Supported entrypoint: \`reef-control-plane serve\` (the image uses it by default).
          EOF
          gh release create "$GITHUB_REF_NAME" \
            --title "@octopus-reef/control-plane ${VERSION}" \
            --notes-file "$RUNNER_TEMP/release-notes.md" \
            --verify-tag
