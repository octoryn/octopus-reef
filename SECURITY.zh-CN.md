[English](SECURITY.md) | **简体中文**

# 安全策略

Reef 是一个*治理*工具 —— 它的全部价值在于:一场会话可以被信任。所以我们对它自身的安全持高标准,并诚实地描述它。

## 报告漏洞

请**私下**报告 —— **不要**为安全问题开公开 issue。请使用本仓库的 [GitHub 私密漏洞报告](https://docs.github.com/en/code-security/security-advisories/guidance-on-reporting-and-writing-information-about-vulnerabilities/privately-reporting-a-security-vulnerability),或发邮件至 `ran@octopusos.ai`。我们力争在数日内确认,并在任何披露之前修复已确认的问题。

## 这些保证究竟是什么

**防篡改 / 验证。** 每场会话是覆盖一条工作主干的哈希链证据日志,两者相互交叉绑定。`verify` / `replay` 以无需信任存储的方式重新推导每一个哈希。

- *无密钥*(默认):可检测意外损坏与局部篡改。它**不能**阻止一个拥有文件访问权、把整场会话重新铸造的写入者 —— 一次完整的重铸会验证通过。
- *有密钥*(`--secret` / `integritySecret`):每个字段与链接都被 HMAC 绑定;没有密钥无法伪造任何字段。对不可信的存储,请使用有密钥模式。

**执行安全。** 真实命令只有在(1)通过 tripwire、(2)通过 `reefAllowlist`(允许已知安全者;`DefaultGate` 的 denylist 只是兜底,不是边界),(3)由受限执行器执行之后才会运行。可选的 `SandboxExecutor`(通过 `--sandbox`)让命令:无 shell、拒绝网络、写入限定在工作区、拒绝读真实 `$HOME` 机密的*内容*、中和 git 配置驱动的代码执行、一次性 `HOME`、进程组超时。

### 诚实的边界

本地沙箱(macOS 上的 `sandbox-exec`;其他平台尽力而为)是**纵深防御,不是关押完全不可信 repo 的牢笼**。一个 repo 自己的配置仍可能执行代码;沙箱会阻止该代码触达机密或网络,但一个掌握新颖的、纯本地攻击向量的坚定攻击者不在本地运行器的范围之内。**对完全不可信的 repo,请在容器里运行 Reef**(`docker run` / `docker compose up`),那里执行由操作系统隔离。这条边界在代码中被强制(当工作区根目录是你的 home 目录时,Reef 拒绝运行命令),并经过十轮对抗式评审加固;我们仍把新发现当作真实问题对待。

### 指挥官的诚实范围

指挥官(`@octopus-reef/agent`)治理一整支*舰队*,并明确它能证明什么、不能证明什么:

- **Worker Ledger。** 整轮运行是一条哈希链式的 `octopus-evidence` 日志;每个 result 都钉住其子会话的链头,因此被调包或被编辑的子会话都会导致验证失败。上文的有/无密钥限制在此同样适用。
- **外部 CLI 不受操作系统沙箱约束。** `cliWorker` 包裹的是一个需要网络与自身鉴权的智能体,所以它被限定在工作区里运行(不在沙箱内)。我们把它的文件**效果**(内容哈希差异)捕获为证据 —— 我们证明*它改了什么*,而非它内部如何推理,也不声称能围困一个恶意的外部 CLI。请把不可信的工作放进容器里跑。
- **BYOK。** 模型密钥属于操作者;Reef 从环境读取它们,除模型端点外不在任何地方持久化或传输它们。

## 范围

在范围内:能验证通过的证据伪造(有密钥模式)、读取机密 / 写出工作区 / 触达网络的沙箱逃逸、服务端静态文件服务中的路径穿越、能验证通过的 Worker Ledger 伪造,以及鉴权/验证绕过。

不在范围内:需要已被攻陷的主机才能实施的攻击;无密钥模式的重铸限制(已文档化,属设计使然);来自敌意本地 repo 的拒绝服务。
