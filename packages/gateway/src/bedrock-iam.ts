/** Compatibility re-export; the shared provider implementation lives in agent. */
export {
  BedrockIamProvider,
  mapBedrockIamResponse as mapResponse,
  type BedrockIamProviderOptions,
} from "@octopus-reef/agent";
