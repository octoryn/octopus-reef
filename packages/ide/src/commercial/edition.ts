import type * as vscode from "vscode";

export const reefEdition = "community";

export function registerCommercialSurface(
  _context: vscode.ExtensionContext,
): vscode.Disposable[] {
  return [];
}
