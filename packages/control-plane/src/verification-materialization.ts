/**
 * Versioned deployment-neutral external materialization Port and bridge.
 *
 * Builder-facing consumers must use opaque refs only. Storage locations,
 * credentials, commands, and secret selectors remain server configuration.
 */
export * from "@octopus-reef/verification/materialization";
